name: First Workflow

on:
  push:
    branches:
      - main
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: print welcome message
        run: "welcome to our first github action workflow"
